cmake_minimum_required(VERSION 3.16)
project(keel LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
add_executable(keel src/main.cpp)
target_include_directories(keel PRIVATE include)
if (NOT MSVC)
  target_compile_options(keel PRIVATE -O3 -Wall -Wextra)
endif()
